Counting
A common operation I might want to perform on tables of data, is simple counting. How many times does a certain value appear? For my dataset for instance, We might want to know how many countries participated in each Olympics, how many women, etc; any column that has categorical data that we can count. The tool  Count occurrences of each record does exactly this.
